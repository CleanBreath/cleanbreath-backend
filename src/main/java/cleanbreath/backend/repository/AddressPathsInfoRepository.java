package cleanbreath.backend.repository;

import cleanbreath.backend.entity.AddressPathsInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AddressPathsInfoRepository extends JpaRepository<AddressPathsInfo, Long> {
}
